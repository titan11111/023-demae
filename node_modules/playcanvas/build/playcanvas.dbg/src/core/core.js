const version = "2.21.3";
const revision = "b1767d5";
function extend(target, ex) {
  for (const prop in ex) {
    const copy = ex[prop];
    if (Array.isArray(copy)) {
      target[prop] = extend([], copy);
    } else if (copy && typeof copy === "object") {
      target[prop] = extend({}, copy);
    } else {
      target[prop] = copy;
    }
  }
  return target;
}
export {
  extend,
  revision,
  version
};
